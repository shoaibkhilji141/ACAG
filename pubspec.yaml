name: acag
description: AI-Based Construction Site Validation System — ACAG / The Urban Unit
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  intl: ^0.20.2
  supabase_flutter: ^2.17.1
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
